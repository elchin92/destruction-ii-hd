/*#include <ddraw.h>

#ifndef __Game__
class Game;
#endif

const double TrMax=10000;

int SetTransparency(LPDIRECTDRAWSURFACE7 Surface, double Transparency);
void DarkenAndSetPalette(Game * TheGame, int Percentage);
void ScaleSurface(LPDIRECTDRAWSURFACE7 * Surface, LPDIRECTDRAWPALETTE * Pal, 
				  int OriginalColor, int DesiredColor,
				  int Skip, int NumberOfColors,
				  int left, int top, int width, int height);*/
